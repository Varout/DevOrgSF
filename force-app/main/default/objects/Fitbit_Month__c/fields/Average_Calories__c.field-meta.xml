<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Average_Calories__c</fullName>
    <externalId>false</externalId>
    <formula>FLOOR(Monthly_Calories__c / (
  /* Find the total amount of days in a month */
  DAY(IF(
    MONTH( Date__c ) = 12,
    DATE( YEAR( Date__c ), 12, 31 ),
    DATE( YEAR( Date__c ), MONTH ( Date__c ) + 1, 1 ) - 1 
  ))
))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Shows the average calories burnt for the month in total.  Will update each time a new Fitbit Day is added.</inlineHelpText>
    <label>Average Calories</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
